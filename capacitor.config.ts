import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cecytem.asistencias',
  appName: 'Cecytem Asistencias',
  webDir: 'dist/cecytem-admin-asistencias/browser'
};

export default config;
